本程序用于实现自动在一台电脑上启动多个MCU。
mcu.exe在本程序的同级目录即可。

自动修改相应配置文件，级联库、Mcu、适配的debug文件。如果要级联，则要求级联库能支持配置级联端口。

如果需要用mcs连接，需要在telnet中设置 setlistenport mcu监听端口号

实现：
监听端口从60000开始以1递增。

Q931 Ras 从1719 1720 以10递增

级联端口从3337开始以1递增